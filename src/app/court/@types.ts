export interface ICourt {
    createdAt: string
    name: string
    avatar: string
    type: string
    sports: string[]
    active: boolean
    id: string
    calendar: string
    events?: any
    _id?: string
}